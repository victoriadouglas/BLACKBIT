<header>
      <div class="navbar">
        <div class="logodiv">
          {% if logo =="blackbit" %}
            <h1 class="home"><a href="{{ '/' | url }}">blackbit</a></h1>
          {% elif logo =="emoji" %}
            <h1 class="home"><a href="{{ '/' | url }}">🤬🤬🤬</a></h1>
          {% elif logo =="emojiskull" %}
            <h1 class="home"><a href="{{ '/' | url }}">💀💀💀</a></h1>
          {% elif logo =="skull" %}
            <a href="{{ '/' | url }}"><img class="navlogo" src="/img/logoskull.svg" alt="skull logo" height="50px;"/></a>
          {% elif logo =="cinnamon" %}
            <a href="{{ '/' | url }}"><img class="navlogo" src="/img/cinnamonnav.svg" alt="cinnamon logo" height="60px;"/></a>
          {% else %}
           <h1 class="home"><a href="{{ '/' | url }}">blackbit</a></h1>
          {% endif %}
        </div>

      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
      <ul class="nav">
      {%- for entry in collections.all | eleventyNavigation %}
        <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">/ {{ entry.title }}</a></li>
      {%- endfor %}
      </ul>
      </div>
    </header>

    

    <div class="container">
    <h1 class="titleheader">{{ title }}</h1>
    <div style="display:flex;flex-wrap: wrap;justify-content: space-between;grid-template-columns: 1fr 1fr;">
    {% if description %}
    <p class="description">{{ description }}</p>
    {% endif %} 

    {% if author %}
    <p class="description">maintained by {{ author }}</p>
    {% endif %} 
    </div>
    </div>

    